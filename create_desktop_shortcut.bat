@echo off
chcp 65001 >nul
title إنشاء اختصار لسطح المكتب - Create Desktop Shortcut
color 0B

echo ═══════════════════════════════════════════════════════════
echo        🏠 إنشاء اختصار على سطح المكتب 🏠
echo ═══════════════════════════════════════════════════════════
echo.

set SCRIPT_PATH=%~dp0START_HERE.bat
set ICON_PATH=%~dp0static\img\logo.ico
set SHORTCUT_NAME=نظام الذهب - Valunshy ERP

echo [1/2] جاري إنشاء الاختصار...

powershell -Command "$s=(New-Object -COM WScript.Shell).CreateShortcut('%USERPROFILE%\Desktop\%SHORTCUT_NAME%.lnk');$s.TargetPath='%SCRIPT_PATH%';$s.WorkingDirectory='%~dp0';if(Test-Path '%ICON_PATH%'){$s.IconLocation='%ICON_PATH%'};$s.Save()"

if errorlevel 1 (
    echo ❌ فشل إنشاء الاختصار
) else (
    echo ✅ تم إنشاء الاختصار بنجاح على سطح المكتب!
    echo يمكنك الآن تشغيل النظام مباشرة من سطح المكتب.
)

echo.
echo ═══════════════════════════════════════════════════════════
pause
