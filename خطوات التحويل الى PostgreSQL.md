# 📄 دليل التحويل إلى قاعدة بيانات PostgreSQL
## نظام Valunshy Jewelry ERP

هذا الدليل يحتوي على الخطوات الدقيقة لتحويل النظام من SQLite إلى PostgreSQL لرفع كفاءة العمل وتعدد المستخدمين.

---

### 🟢 الخطوة 1: تثبيت برنامج PostgreSQL السيرفر
1. قم بتحميل النسخة المناسبة لوندوز من الموقع الرسمي: [PostgreSQL Downloads](https://www.enterprisedb.com/downloads/postgres-postgresql-downloads)
2. أثناء التثبيت، سيطلب منك كلمة سر (Password) للمستخدم الرئيسي `postgres` - **احفظها جيداً**.
3. اترك المنفذ (Port) كما هو: `5432`.
4. بعد التثبيت، افتح برنامج **pgAdmin 4** (يأتي مع التثبيت) وأنشئ قاعدة بيانات جديدة باسم `gold_erp_db`.

---

### 🟡 الخطوة 2: تثبيت المكتبات اللازمة في النظام
افتح نافذة الأوامر (Command Prompt) داخل مجلد البرنامج وقم بتشغيل الأمر التالي:
```bash
pip install psycopg2-binary
```

---

### 🔴 الخطوة 3: تصدير البيانات من النظام الحالي (من SQLite)
قبل تغيير الإعدادات، يجب سحب البيانات الحالية حتى لا تضيع:
1. تأكد أنك داخل مجلد البرنامج.
2. قم بتشغيل الأمر التالي:
```bash
python manage.py dumpdata --exclude contenttypes --exclude auth.Permission --indent 2 > all_data.json
```
*الآن بياناتك محفوظة في ملف باسم `all_data.json`.*

---

### ⚙️ الخطوة 4: تعديل إعدادات البرنامج (settings.py)
افتح ملف `backend/settings.py` وغير جزء `DATABASES` ليكون كالتالي:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'gold_erp_db',       # الاسم الذي اخترته في الخطوة 1
        'USER': 'postgres',          # المستخدم الافتراضي
        'PASSWORD': 'your_password', # ككلة السر التي اخترتها في الخطوة 1
        'HOST': '127.0.0.1',
        'PORT': '5432',
    }
}
```

---

### 🔵 الخطوة 5: إنشاء الجداول في PostgreSQL
الآن سنقوم ببناء بيوت البيانات في القاعدة الجديدة:
```bash
python manage.py migrate --run-syncdb
```

---

### 📥 الخطوة 6: استيراد البيانات إلى PostgreSQL
الخطوة الأخيرة هي إعادة البيانات التي سحبناها في الخطوة 3:
1. قم بتشغيل الأمر التالي:
```bash
python manage.py loaddata all_data.json
```

---

### ✅ الخطوة 7: التأكد والتشغيل
شغل النظام الآن كالمعتاد:
```bash
python manage.py runserver
```

---

### ⚠️ ملاحظات هامة:
*   **النسخ الاحتياطي:** دائماً احتفظ بنسخة من ملف `db.sqlite3` قبل البدء.
*   **الأداء:** ستلاحظ سرعة أكبر خاصة عند فتح النظام من أكثر من جهاز في نفس الوقت.
*   **الشبكة:** إذا أردت فتح النظام من أجهزة أخرى، تأكد من تعديل ملف `pg_hba.conf` في إعدادات PostgreSQL للسماح بالاتصالات الخارجية.

---
**تم إعداد هذا الدليل بطلب من العميل - ديسمبر 2025**
